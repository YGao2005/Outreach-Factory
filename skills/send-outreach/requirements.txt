# send-outreach dependencies
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.197.0
# Match orchestrator/requirements.txt's spec exactly so installing both files
# never trips pip's resolver: the old exact pin here vs the core's range forced
# a downgrade and printed a dependency-conflict warning.
pyyaml>=6.0

# OpenTelemetry is NO LONGER a core send dependency. The send path uses the
# no-op span shim orchestrator/obs.py by default, so a cold send needs neither
# the opentelemetry SDK nor the heavy observability module. Opt in to real OTel
# spans + the Prometheus exporter by setting OUTREACH_FACTORY_OTEL=1 and
# installing the advanced/operations deps in orchestrator/requirements.txt.
# (Enforced by tests/test_import_graph_lean.py.)

# .env loading for the unified secret loader (orchestrator/env_loader.py). Soft
# dependency: the loader falls back to a built-in parser if it is absent. Floor
# raised to the patched version per CVE-2026-28684 (GHSA-mf9w-mj56-hr94), fixed
# in python-dotenv 1.2.2 (mirrors orchestrator/requirements.txt).
python-dotenv>=1.2.2
