# Test + dev dependencies (runtime deps live in orchestrator/requirements.txt
# and skills/send-outreach/requirements.txt).
#
# Install: pip install -r requirements-dev.txt

# Floor raised to the patched version: CVE-2025-71176 (GHSA-6w46-j5rx-g56g),
# a predictable /tmp/pytest-of-{user} DoS/priv-esc, is fixed in pytest 9.0.3.
pytest>=9.0.3
hypothesis>=6.155.1    # property-based testing (Pillar A cooldown DST safety, etc.)
